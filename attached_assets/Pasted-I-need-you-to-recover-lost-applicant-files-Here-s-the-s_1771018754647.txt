I need you to recover lost applicant files. Here's the situation:
169 applicant upload files (IDs, paystubs, W2s) are referenced in the database table rental_submission_files but the actual files may exist on disk at uploads/applicants/. I need you to upload them to permanent cloud storage and update the database.
Step 1: Check how many files exist on disk:
ls uploads/applicants/ | wc -l
If it's more than 27, we have recovered files. If 27 or fewer, this checkpoint doesn't have them — tell me to try a different one.
Step 2: Download the mapping file from cloud storage:
mkdir -p .private/backups && npx tsx -e "
async function main() {
  const { objectStorageClient } = await import('./server/replit_integrations/object_storage/objectStorage');
  const PRIVATE_OBJECT_DIR = process.env.PRIVATE_OBJECT_DIR || '';
  const objectPath = PRIVATE_OBJECT_DIR + '/backups/file-mapping-1771017306730.json';
  const normalized = objectPath.startsWith('/') ? objectPath : '/' + objectPath;
  const parts = normalized.split('/');
  const bucket = objectStorageClient.bucket(parts[1]);
  const file = bucket.file(parts.slice(2).join('/'));
  const [contents] = await file.download();
  const fs = await import('fs');
  fs.writeFileSync('.private/backups/file-mapping-1771017306730.json', contents);
  const data = JSON.parse(contents.toString());
  console.log('Downloaded mapping: ' + data.rentalSubmissionFiles.length + ' records');
}
main();
"
Step 3: For each of the 169 records in that mapping, check if the file exists at uploads/applicants/{uuid}.{ext}. For every file found, upload it to Replit Object Storage at PRIVATE_OBJECT_DIR/applicants/{uuid}.{ext} using the existing objectStorageClient from server/replit_integrations/object_storage/objectStorage, then update rental_submission_files.stored_path in the database to the new cloud path. Write a script to do this. Use the app's existing db from server/db and rentalSubmissionFiles from shared/schema with drizzle eq for updates. Skip any records whose stored_path already starts with /replit-objstore. Generate a CSV report of results.
Step 4: After the script finishes, tell me the results (how many recovered, how many still missing).
